{
  "name": "tohc-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:mschf": "cross-env BASE_PATH=/mschf astro build",
    "preview": "astro preview",
    "push": "pwsh -File scripts/deploy.ps1",
    "push:mschf": "cross-env BASE_PATH=/mschf astro build && pwsh -File scripts/deploy.ps1",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.2.8",
    "gsap": "^3.15.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
